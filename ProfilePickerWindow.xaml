<Window x:Class="WebView2Automation.ProfilePickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="KaHoops Arena — Choose a profile"
        Icon="app.ico"
        Height="470" Width="420"
        MinHeight="420" MinWidth="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        ShowInTaskbar="True"
        Background="{StaticResource OpaqueFallback}"
        TextOptions.TextFormattingMode="Display"
        UseLayoutRounding="True">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,4">
            <Image Source="Assets/logo.png" Width="26" Height="26"
                   VerticalAlignment="Center" Margin="0,0,10,0"
                   RenderOptions.BitmapScalingMode="HighQuality"/>
            <TextBlock Text="Choose a profile"
                       Foreground="{StaticResource Fg}"
                       FontSize="17" FontWeight="SemiBold"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <TextBlock Grid.Row="1"
                   Text="Each profile keeps its own login and browsing data. Use separate profiles to run more than one account at the same time."
                   Style="{StaticResource FieldHint}"
                   FontSize="11"
                   Margin="0,0,0,14"/>

        <!-- Existing profiles -->
        <Border Grid.Row="2"
                Background="#59000000"
                BorderBrush="{StaticResource BorderSoft}"
                BorderThickness="1"
                CornerRadius="6">
            <ListBox x:Name="ProfileList"
                     Background="Transparent"
                     BorderThickness="0"
                     Foreground="{StaticResource Fg}"
                     Padding="4"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     MouseDoubleClick="ProfileList_MouseDoubleClick"
                     SelectionChanged="ProfileList_SelectionChanged">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="10,8"/>
                        <Setter Property="Margin"  Value="2,1"/>
                        <Setter Property="Foreground" Value="{StaticResource Fg}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="Bd"
                                            Background="Transparent"
                                            CornerRadius="5"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#1AFFFFFF"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentSoftGradient}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0"
                                       Text="{Binding Name}"
                                       FontSize="12.5"
                                       VerticalAlignment="Center"/>

                            <Border Grid.Column="1"
                                    Background="#2EF06E6E"
                                    CornerRadius="3"
                                    Padding="6,2"
                                    Visibility="{Binding InUseVisibility}">
                                <TextBlock Text="in use"
                                           FontSize="10"
                                           Foreground="#FFF06E6E"/>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- Create new -->
        <StackPanel Grid.Row="3" Margin="0,14,0,0">
            <TextBlock Text="Or create a new one" Style="{StaticResource FieldLabel}"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="NewProfileBox"
                         Grid.Column="0"
                         KeyDown="NewProfileBox_KeyDown"
                         TextChanged="NewProfileBox_TextChanged"
                         ToolTip="A short name, for example: Work, Alt, Testing"/>

                <Button x:Name="CreateButton"
                        Grid.Column="2"
                        Content="Create"
                        Style="{StaticResource GhostButton}"
                        Padding="14,7"
                        Click="CreateButton_Click"/>
            </Grid>
        </StackPanel>

        <TextBlock x:Name="MessageText"
                   Grid.Row="4"
                   Style="{StaticResource FieldHint}"
                   Foreground="#FFF06E6E"
                   Margin="0,10,0,0"
                   Text=""/>

        <!-- Actions -->
        <Grid Grid.Row="5" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="CancelButton"
                    Grid.Column="0"
                    Content="Cancel"
                    Style="{StaticResource GhostButton}"
                    Padding="16,9"
                    IsCancel="True"
                    Click="CancelButton_Click"/>

            <Button x:Name="OpenButton"
                    Grid.Column="2"
                    Content="Open profile"
                    Padding="20,9"
                    IsDefault="True"
                    Click="OpenButton_Click"/>
        </Grid>
    </Grid>
</Window>
